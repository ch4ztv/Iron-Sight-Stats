# CSS folder

Planned files:
- `app.css` — global theme, layout, typography, responsive rules
- optional section or component CSS files only if needed later

Recommendation:
Keep the public app styles centralized at first, then split only when justified.
