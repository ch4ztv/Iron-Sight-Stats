# Shared components

Suggested modules:
- `cards.js`
- `tables.js`
- `filters.js`
- `media.js`
- `modals.js` only if truly needed

Goal:
Build a reusable public UI system instead of section-specific one-off patterns.
